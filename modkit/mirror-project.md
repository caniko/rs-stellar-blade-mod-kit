# Mirror Project

No mirror Unreal project is committed in v1. Create one only after confirming the exact Stellar Blade PC engine version, plugin requirements, and legal constraints.

Generated UHT-compatible headers, SDK dumps, CXX dumps, and `.usmap` files are local evidence and must not be committed by default.
